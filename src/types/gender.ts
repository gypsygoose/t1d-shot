// Which body silhouette (src/components/BodyImage/) renders behind the
// injection points — purely cosmetic, has no effect on point coloring/
// behavior (see CLAUDE.md's "Gender").
export enum Gender {
  Male = 'male',
  Female = 'female',
}
