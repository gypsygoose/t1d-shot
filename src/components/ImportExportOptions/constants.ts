import { ExportMarksKey, ExportSettingKey } from "../../types";

export const SETTING_KEYS: ExportSettingKey[] = Object.values(ExportSettingKey);
export const MARKS_KEYS: ExportMarksKey[] = Object.values(ExportMarksKey);
